---
- name: Converge
  hosts: all
  tasks:
    - debug:
        msg: "molecule_instance_config {{ molecule_instance_config }}"

    - name: Creating a file with content
      copy:
        dest: "/home/rpelisse/hello.txt"
        content: |
         "hello"
